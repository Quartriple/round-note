# =========================================================
# 환경 변수 템플릿
# =========================================================
# 사용법: 이 파일을 .env로 복사하고 실제 값을 입력하세요
# cp .env.example .env

# ==================== Database & Cache ====================
# Docker Compose 내부 서비스 연결 정보
DATABASE_URL="postgresql://roundnote_user:roundnote_password@db:5432/roundnote_db"
REDIS_URL="redis://redis:6379"

# ==================== External APIs ====================
# 외부 API 인증 키 (실제 키로 교체하세요)
DEEPGRAM_API_KEY="your_deepgram_api_key_here"
ELEVENLABS_API_KEY="your_elevenlabs_api_key_here"
OPENAI_API_KEY="your_openai_api_key_here"

# ==================== NCP Object Storage ====================
# NCP Object Storage 설정 (실제 값으로 교체하세요)
NCP_ENDPOINT_URL="https://kr.object.ncloudstorage.com/your-bucket-name"
NCP_BUCKET_NAME="your-bucket-name"
NCP_ACCESS_KEY="your_ncp_access_key_here"
NCP_SECRET_KEY="your_ncp_secret_key_here"

# ==================== CORS ====================
# CORS 설정
CORS_ORIGIN_LOCAL="http://localhost:3000"
CORS_ORIGIN="https://your-production-domain.com"

# ==================== JWT & Security ====================
# JWT 인증 (보안을 위해 반드시 변경하세요)
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production
JWT_ALGORITHM=HS256
SECRET_KEY="your-secret-key-here-please-change"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== OAuth (Social Login) ====================
# Google OAuth (실제 값으로 교체하세요)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback
# Naver OAuth (추후 구현 시 주석 해제)
# NAVER_CLIENT_ID=your-naver-client-id
# NAVER_CLIENT_SECRET=your-naver-client-secret
# NAVER_REDIRECT_URI=http://localhost:8000/api/v1/auth/naver/callback
# Kakao OAuth (추후 구현 시 주석 해제)
# KAKAO_CLIENT_ID=your-kakao-client-id
# KAKAO_CLIENT_SECRET=your-kakao-client-secret
# KAKAO_REDIRECT_URI=http://localhost:8000/api/v1/auth/kakao/callback

# ==================== Encryption ====================
# Encryption (보안을 위해 반드시 변경하세요)
# 생성 방법: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY="your_base64_encryption_key_here"

# ==================== Logging Configuration (Phase 1) ====================
# Environment: development | production
ENVIRONMENT=development
LOG_LEVEL=INFO

# ==================== Retry Configuration (Phase 2) ====================
# Maximum number of retry attempts for external API calls
RETRY_MAX_ATTEMPTS=3
# Minimum wait time between retries (seconds)
RETRY_MIN_WAIT_SECONDS=2
# Maximum wait time between retries (seconds)
RETRY_MAX_WAIT_SECONDS=10
# Exponential backoff multiplier
RETRY_MULTIPLIER=1